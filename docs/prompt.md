# Prompt

Paste the one-shot prompt here before any generation run.

This file is the prompt to give each model, together with `docs/PRD.md`.
Every model folder should be produced from this prompt and then left as a
frozen first-shot snapshot.
